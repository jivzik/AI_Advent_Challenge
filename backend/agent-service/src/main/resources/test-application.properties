# Test configuration
spring.profiles.active=test

# Disable scheduler for manual testing
code-review.scheduler.enabled=false

# Use test database
spring.datasource.url=jdbc:postgresql://localhost:5432/agent_service_test
spring.datasource.username=postgres
spring.datasource.password=postgres

# Test repository
code-review.repository=your-username/your-repo

# GitHub token (if testing GitHub integration)
github.token=${GITHUB_TOKEN}

# Logging
logging.level.de.jivz.agentservice=DEBUG